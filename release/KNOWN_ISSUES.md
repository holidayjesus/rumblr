# Known Issues

- Sleep/wake and flaky-network soak testing still needs long real-world runs on multiple networks.
- Some uncommon IRC numerics may still fall back to generic server messages.
- First-run onboarding uses built-in network presets; custom networks are configured from Options.
- The app is still labeled alpha, so config shape may change between builds.
